spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/car_rental?characterEncoding=utf-8&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    type: com.zaxxer.hikari.HikariDataSource
    driver-class-name: com.mysql.jdbc.Driver
    username: root
    password: root

mybatis:
  mapper-locations: classpath:/mapper/**/*.xml





server:
  port: 8080
logging:
  level:
    com.bjsxt.mapper: TRACE
